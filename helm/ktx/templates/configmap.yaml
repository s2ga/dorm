apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "ktx.fullname" . }}
  labels:
    {{- include "ktx.labels" . | nindent 4 }}
data:
  {{- range $k, $v := .Values.config }}
  {{ $k }}: {{ $v | quote }}
  {{- end }}
